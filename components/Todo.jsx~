import React, {Component, PropTypes} from 'react'

export default class Todo extends Componenet{
    render(){
        return(
            <li>
                {this.props.text}
            </li>
        )
    }
}
